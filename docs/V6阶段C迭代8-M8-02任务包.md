# V6 阶段C迭代8（M8-02）任务包（已完成）

## 1. 当前阶段判定
- 当前阶段：`阶段C迭代8（M8-02）已完成`。
- 进入条件：`M8-01` 已完成，`M7` 高保真原型中的 `MINI-REPORT-01` 已冻结。

## 2. 迭代8第二批范围（M8-02 小程序轻量报表）
- 目标：初始化 `miniprogram/` 工程，并落地 `MINI-REPORT-01 经营快报` 页面。
- 模块边界：本轮只处理小程序工程最小基座、轻量报表页、角色可见性校验、演示数据接入与纯函数测试；不进入订单发起、待办、执行回执、登录鉴权、消息中心。
- 首批实现边界：
  - 初始化小程序 `app.js / app.json / app.wxss / project.config.json`。
  - 实现 `pages/report/index` 轻量报表页。
  - 固化首版四卡与异常提醒卡，展示 `口径版本`、`更新时间`、`SLA 状态`。
  - 按规则24/25/30落地角色边界：仅运营、财务、管理员可见该页数据。
  - 提供演示模式，保证在登录链路未落地前页面可预览、可评审。
- 本轮不实现：
  - 微信登录与 Token 链路
  - 后端身份网关透传
  - 订单、待办、仓库执行、消息中心页面
  - 小程序业务看板独立页面

## 2.1 M8-02 子模块拆分

| 子模块ID | 子模块名称 | 输入 | 输出 | 依赖 | 状态流转 | 失败回滚 |
|---|---|---|---|---|---|---|
| M8-02-01 | 小程序工程初始化 | 阶段A/B 原型与 V5 目录结构参考 | `miniprogram` 可在微信开发者工具打开 | M7 | 工程可预览 | 删除新增文件 |
| M8-02-02 | 轻量报表页落地 | `/reports/light/overview` 契约 + 原型说明 | `MINI-REPORT-01` 页面 | M7 | 只读展示 | 页面回滚 |
| M8-02-03 | 角色边界与演示模式 | 角色冻结规则 | 运营侧可见、非运营侧阻断展示 | M1/M7 | 页面可评审 | 配置回滚 |
| M8-02-04 | 格式化与页面纯函数测试 | 页面展示口径 | 可重复回归的 Node 测试 | M8-02-02 | 测试通过 | 删除测试文件 |

## 2.2 M8-02 实施约束
- 小程序所有用户可见信息必须中文显示。
- `MINI-REPORT-01` 只允许运营、财务、管理员（小程序）访问；客户、供应商、仓库仅展示禁止访问提示，不暴露经营金额汇总。
- 页面视觉必须遵循 `docs/V6阶段C迭代7-M7高保真原型说明.md`，不得改写指标名称、版本文案与 SLA 文案。
- 当前仅提供演示模式；真实联调待 `M8-03` 登录与身份链路落地后接入。
- 本轮不引入第三方小程序 UI 库，维持原生小程序执行链。

## 3. 迭代8第二批测试清单

| 用例ID | 用例名称 | 预期结果 |
|---|---|---|
| M8-02-T01 | 小程序工程结构完整 | `app/project/page` 配置文件完整且路径可解析 |
| M8-02-T02 | 轻量报表页语法检查 | 所有 `.js` 文件可通过 `node --check` |
| M8-02-T03 | 格式化工具测试 | 金额/数量/时间格式化结果正确 |
| M8-02-T04 | 角色边界测试 | 非运营侧角色返回不可见 |
| M8-02-T05 | 异常分桶测试 | 三类异常文案与计数正确 |
| M8-02-T06 | 文档一致性检查 | README、任务包、页面实现口径一致 |

## 4. 迭代8第二批完成定义（DoD）
- `miniprogram/` 已从占位目录升级为标准小程序工程结构。
- `MINI-REPORT-01` 页面可在演示模式下展示四卡与异常提醒。
- 角色边界、空态、延迟态、错误态均为中文显示。
- 本轮变更具备自检记录与独立循环评审记录。

## 5. 本轮完成记录
- `M8-02-01 ~ M8-02-04` 已全部落地。
- `M8-02-T01 ~ M8-02-T06` 已覆盖并通过。
- 已完成：
  - 小程序工程初始化
  - `MINI-REPORT-01` 轻量报表页
  - 演示模式数据接入
  - 角色边界阻断展示
  - 格式化与页面纯函数测试
- 当前仍未实现：
  - 微信登录与身份网关
  - 小程序业务看板独立页
  - 订单/待办/执行回执页面
