# V6 阶段D D-01联调执行记录（2026-03-08）

## 1. 文档定位
- 文档状态：`生效`
- 目标：记录阶段 D 启动后的首轮自动化回归基线、问题分级与后续联调入口，作为 `D-01 联调与综合回归执行` 的执行留痕。
- 依据：`docs/V6阶段D-联调与综合回归清单.md`、`docs/V6项目总控台账.md`。

## 2. 本轮执行范围
- 后端自动化回归：执行阶段 D 清单列出的 `M1 ~ M8` 后端主链测试集。
- 管理后台自动化回归：执行 `pnpm test`。
- 管理后台构建门禁：执行 `pnpm build`。
- `D-CHAIN-01` 定向回归：执行“合同驳回回退草稿后修改再提审 -> 合同生效 -> 客户下单 -> 运营审批 -> 财务审批 -> 销售转采购”后端联调测试，以及 `ContractsView`、`OrdersView` 前端审批交互回归。
- `D-CHAIN-02` 定向回归：执行“待补录 -> 确认 -> 凭证路径 -> 退款申请/审核/驳回 -> 核销”后端联调测试，以及 `FundsView`、`FundsReconcileView` 前端交互回归。
- `D-CHAIN-03` 定向回归：执行“入库过账 -> 出库生成/提交 -> 校验失败 -> 仓库身份边界 -> 数量履约累计一致”后端联调测试，以及 `InventoryView` 只读回看交互回归。
- `D-CHAIN-04` 定向回归：执行“自动关闭 -> 手工关闭 -> 差异展示 -> 关闭后阻断一致”后端联调测试，以及 `ContractCloseView` 回看与手工关闭交互回归。
- `D-CHAIN-05` 定向回归：执行“仪表盘 -> 业务看板 -> 轻量报表 -> 多维报表 -> 导出任务 -> 汇总重算 -> 扫描告警留痕”后端联调测试，以及 `OverviewView`、`TasksView`、`ReportExportTasksView` 报表回看与任务中心交互回归。
- `D-CHAIN-06` 定向回归：执行“本地联调登录 -> 会话续期 -> 轻量报表 -> 微信登录绑定判断 -> 待办/消息/经营快报页面”后端与小程序自动化回归。
- 本轮未覆盖：管理后台全页手工联调、小程序身份切换与真实登录链路手工回归、端到端同批次经营链路手工核对。

## 3. 执行结果

| 类别 | 命令 | 结果 |
|---|---|---|
| 后端自动化回归 | `conda run -n jgport python -m pytest backend/tests/test_m1_access.py backend/tests/test_m1_thresholds.py backend/tests/test_m1_audit.py backend/tests/test_d01_contract_order_chain.py backend/tests/test_d01_funds_chain.py backend/tests/test_d01_inventory_chain.py backend/tests/test_d01_contract_close_chain.py backend/tests/test_d01_reports_chain.py backend/tests/test_d01_mini_identity_chain.py backend/tests/test_m2_contracts.py backend/tests/test_m3_orders.py backend/tests/test_m4_funds.py backend/tests/test_m5_inventory.py backend/tests/test_m6_contract_close.py backend/tests/test_m7_reports.py backend/tests/test_m8_mini_auth.py backend/tests/test_m8_wechat_auth.py backend/tests/test_m8_mini_orders.py backend/tests/test_m8_supplier_purchase_orders.py` | `137 passed` |
| 管理后台自动化回归（初始基线） | `pnpm test`（连续 `3` 次复跑） | 每次均 `55 passed` |
| 管理后台构建门禁 | `pnpm build` | 通过 |
| `D-CHAIN-01` 定向后端回归 | `conda run -n jgport python -m pytest backend/tests/test_d01_contract_order_chain.py backend/tests/test_m2_contracts.py backend/tests/test_m3_orders.py` | `25 passed` |
| `D-CHAIN-01` 定向前端回归 | `pnpm test && pnpm build && pnpm test` | 两次测试均 `61 passed`，构建通过 |
| `D-CHAIN-02` 定向后端回归 | `conda run -n jgport python -m pytest backend/tests/test_d01_funds_chain.py backend/tests/test_m4_funds.py` | `24 passed` |
| `D-CHAIN-02` 定向前端回归 | `pnpm test && pnpm build && pnpm test` | 两次测试均 `68 passed`，构建通过 |
| `D-CHAIN-03` 定向后端回归 | `conda run -n jgport python -m pytest backend/tests/test_d01_inventory_chain.py backend/tests/test_m5_inventory.py` | `15 passed` |
| `D-CHAIN-03` 定向前端回归 | `pnpm test && pnpm build` | `72 passed`，构建通过 |
| `D-CHAIN-04` 定向后端回归 | `conda run -n jgport python -m pytest backend/tests/test_d01_contract_close_chain.py backend/tests/test_m6_contract_close.py` | `10 passed` |
| `D-CHAIN-04` 定向前端回归 | `pnpm exec vitest run src/views/contract-close/ContractCloseView.spec.ts src/mock/contract-close.spec.ts` | `7 passed` |
| `D-CHAIN-05` 定向后端回归 | `conda run -n jgport python -m pytest backend/tests/test_d01_reports_chain.py backend/tests/test_m7_reports.py` | `17 passed` |
| `D-CHAIN-05` 定向前端回归 | `pnpm exec vitest run src/views/dashboard/OverviewView.spec.ts src/views/board/TasksView.spec.ts src/views/report-export-tasks/ReportExportTasksView.spec.ts src/views/reports-multi-dim/ReportsMultiDimView.spec.ts src/views/report-recompute-tasks/ReportRecomputeTasksView.spec.ts src/stores/report.spec.ts` | `13 passed` |
| 管理后台自动化回归（更新后） | `pnpm test` | `82 passed` |
| 管理后台构建门禁（更新后） | `pnpm build` | 通过 |
| `D-CHAIN-06` 定向后端回归 | `conda run -n jgport python -m pytest backend/tests/test_d01_mini_identity_chain.py backend/tests/test_m8_mini_auth.py backend/tests/test_m8_wechat_auth.py backend/tests/test_m8_mini_orders.py backend/tests/test_m8_supplier_purchase_orders.py` | `30 passed` |
| `D-CHAIN-06` 小程序语法校验与自动化回归 | `cd miniprogram && node --check app.js config/env.js mocks/order.js utils/api.js utils/format.js utils/light-report.js utils/message.js utils/navigation.js utils/order.js utils/request.js utils/session.js utils/supplier-purchase.js utils/todo.js utils/warehouse-exec.js pages/login/index.js pages/todo/index.js pages/msg/index.js pages/order/index.js pages/report/index.js pages/exec/index.js pages/supplier-purchase/index.js && node --test tests/*.test.js` | 语法校验通过，`58 passed` |

## 4. 问题分级结论
- 阻断问题：无。
- 高优先级问题：无。
- 一般问题：无新增自动化失败项。
- 稳定性复核：已针对独立评审中的复跑差异做连续复测，当前未复现前端失败。
- 计数说明：初始自动化基线阶段管理后台全量为 `55 passed`；补入 `OrdersView` 新增回归与权限边界用例后，当前管理后台全量更新为 `61 passed`。
- 计数说明补充：补入 `FundsView`、`FundsReconcileView` 定向回归与权限边界用例后，当前管理后台全量更新为 `68 passed`。
- 计数说明补充：补入 `InventoryView` 仓储执行只读回看回归后，当前管理后台全量更新为 `72 passed`；后端全量因纳入 `test_d01_inventory_chain.py` 更新为 `131 passed`。
- 计数说明补充：补入 `ContractCloseView` 合同关闭主链回归后，当前管理后台全量更新为 `76 passed`；后端全量因纳入 `test_d01_contract_close_chain.py` 更新为 `133 passed`。
- 计数说明补充：补入 `test_d01_reports_chain.py`、`OverviewView.spec.ts`、`TasksView.spec.ts`、`ReportExportTasksView.spec.ts` 后，当前管理后台全量更新为 `82 passed`；后端全量因纳入 `test_d01_reports_chain.py` 更新为 `135 passed`。
- 计数说明补充：补入 `test_d01_mini_identity_chain.py` 后，后端全量基线进一步更新为 `137 passed`，`D-CHAIN-06` 定向后端回归更新为 `30 passed`；同步补入 `miniprogram/tests/login-page.test.js`、`miniprogram/tests/todo-page.test.js`、`miniprogram/tests/message-page.test.js`、`miniprogram/tests/report-page.test.js` 与 `miniprogram/tests/page-harness.js` 后，小程序自动化当前为 `58 passed`。
- 当前判断：阶段 D 自动化门禁基线通过，且 `D-CHAIN-01`、`D-CHAIN-02`、`D-CHAIN-03`、`D-CHAIN-04`、`D-CHAIN-05`、`D-CHAIN-06` 已完成定向回归验证，可继续进入手工联调与同批次链路核对。

## 5. `D-CHAIN-01` 验证结论
- 验证结果：已通过。
- 后端链路结论：已覆盖销售合同驳回回退草稿后修改再提审、审批生效、客户下单、订单提交、运营审批、财务审批、销售转采购生成采购订单。
- 前端链路结论：`ContractsView` 与新增 `OrdersView` 审批交互回归通过，管理后台角色按钮边界与审批提交流程可回归。
- 风险结论：当前未发现阻断 `D-CHAIN-01` 继续手工联调的自动化问题。

## 6. `D-CHAIN-02` 验证结论
- 验证结果：已通过。
- 后端链路结论：已覆盖收款待补录转确认、付款确认与凭证路径留痕、付款退款申请/驳回/再申请/审核通过、付款单核销状态流转。
- 前端链路结论：新增 `FundsView.actions.spec.ts` 与 `FundsReconcileView.actions.spec.ts`，已覆盖资金补录、单据确认、退款申请、退款审核、退款驳回、核销与只读角色阻断。
- 风险结论：当前未发现阻断 `D-CHAIN-02` 继续手工联调的自动化问题。

## 7. `D-CHAIN-03` 验证结论
- 验证结果：已通过。
- 后端链路结论：新增 `backend/tests/test_d01_inventory_chain.py`，已覆盖入库单过账、系统出库生成与提交、手工出库校验失败、客户越权阻断、数量履约累计一致并最终推进到 `数量履约完成`。
- 前端链路结论：新增 `admin-web/src/views/inventory/InventoryView.spec.ts`，已覆盖入库/出库切换、状态与来源筛选、校验失败提示、系统回执号与手工回执号展示差异、已过账只读回看提示。
- 风险结论：当前未发现阻断 `D-CHAIN-03` 继续手工联调的自动化问题。

## 8. `D-CHAIN-04` 验证结论
- 验证结果：已通过。
- 后端链路结论：新增 `backend/tests/test_d01_contract_close_chain.py`，已覆盖自动关闭收口、手工关闭差异金额与数量留痕、关闭后终止未完成单据，以及关闭后资金补录和仓储执行阻断一致。
- 前端链路结论：新增 `admin-web/src/views/contract-close/ContractCloseView.spec.ts`，并修正 `admin-web/src/views/contract-close/ContractCloseView.vue` 的差异数据兼容与手工关闭角色门禁，已覆盖自动关闭回看、筛选、后端差异字段展示兼容、手工关闭成功与运营只读阻断。
- 风险结论：当前未发现阻断 `D-CHAIN-04` 继续手工联调的自动化问题。

## 9. `D-CHAIN-05` 验证结论
- 验证结果：已通过。
- 后端链路结论：新增 `backend/tests/test_d01_reports_chain.py`，串联验证仪表盘、业务看板、轻量报表、多维报表、导出任务、汇总重算与扫描告警留痕，并按基线差量定向清理本用例新增的报表快照、任务与扫描留痕，避免共享测试库污染后续用例。
- 前端链路结论：新增 `admin-web/src/views/dashboard/OverviewView.spec.ts`、`admin-web/src/views/board/TasksView.spec.ts`、`admin-web/src/views/report-export-tasks/ReportExportTasksView.spec.ts`，结合既有 `ReportsMultiDimView.spec.ts` 与 `ReportRecomputeTasksView.spec.ts`，已覆盖报表首页加载、业务看板统计、导出任务中心下载/重试、只读角色边界与汇总重算回看。
- 风险结论：当前未发现阻断 `D-CHAIN-05` 继续手工联调的自动化问题。

## 10. `D-CHAIN-06` 验证结论
- 验证结果：已通过。
- 后端链路结论：新增 `backend/tests/test_d01_mini_identity_chain.py`，已覆盖本地联调登录、访问身份回读、会话续期、轻量报表权限边界，以及微信登录未绑定/已绑定两条身份路径。
- 小程序链路结论：新增 `miniprogram/tests/login-page.test.js`、`miniprogram/tests/todo-page.test.js`、`miniprogram/tests/message-page.test.js`、`miniprogram/tests/report-page.test.js` 与 `miniprogram/tests/page-harness.js`，已覆盖本地联调登录跳转、微信未绑定提示、供应商待办入口、消息中心已读状态、经营快报异常聚焦与客户角色权限边界；供应商采购单页面能力与权限继续由既有 `miniprogram/tests/supplier-purchase.test.js`、`miniprogram/tests/supplier-purchase-page.test.js` 共同覆盖。
- 风险结论：当前未发现阻断 `D-CHAIN-06` 继续手工联调的自动化问题。

## 11. 后续联调入口
- 管理后台全页与跨页手工联调仍待执行，需结合同批次 `CODEX-TEST-` 数据继续核对页面提示、按钮可见性与异常态。
- 联调新增测试数据必须带 `CODEX-TEST-` 或 `AUTO-TEST-` 标识，联调完成后按标识定向清理。
