# V6 阶段D手工联调检查清单（2026-03-08）

## 1. 文档定位
- 文档状态：`生效`
- 目标：把阶段 D 剩余的“管理后台全页与跨页手工联调、小程序身份切换与真实登录链路手工回归、端到端同批次经营链路核对、审计追溯回看”拆成可执行清单，避免手工联调时遗漏路径、角色与留痕口径。
- 使用边界：本清单只用于阶段 D 手工联调执行，不替代 `docs/V6阶段D-联调与综合回归清单.md`、`docs/需求方案.md` 与 `docs/history/stage-d/V6阶段D-D01联调执行记录-2026-03-08.md`。

## 2. 执行前提
- 自动化门禁前置：后端全量 `137 passed`、管理后台 `pnpm test` 为 `86 passed`、管理后台 `pnpm build` 通过、小程序 `node --test tests/*.test.js` 为 `58 passed`。
- 联调范围前置：仅验证阶段 D 冻结范围内的真实交付链路，不新增页面、不新增接口、不扩张业务规则。
- 数据前置：同一批次手工联调数据统一使用唯一前缀，推荐 `CODEX-TEST-D01-MANUAL-20260308`。
- 留痕前置：执行结果、问题分级、补充说明统一回写 `docs/history/stage-d/V6阶段D-D01联调执行记录-2026-03-08.md`。

## 3. 联调环境准备

### 3.1 后端
```bash
cd backend
conda activate jgport
alembic upgrade head
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 3.2 管理后台
```bash
cd admin-web
cp .env.example .env
pnpm install
pnpm dev
```

- 联调建议环境变量：
  - `VITE_REPORTS_MODE=proxy`
  - `BACKEND_API_BASE_URL=http://127.0.0.1:8000/api/v1`
  - `BACKEND_AUTH_SECRET=<后端代理共享密钥>`

### 3.3 小程序
- 使用微信开发者工具打开 `miniprogram/`。
- 打开本地调试能力，允许开发环境不校验请求域名 / TLS。
- 本地联调模式访问地址默认为 `http://127.0.0.1:8000/api/v1`。

## 4. 同批次数据与清理约定
- 合同号、订单号、备注、附件路径、公司编码统一带前缀：`CODEX-TEST-D01-MANUAL-20260308`。
- 推荐命名：
  - 销售合同：`CODEX-TEST-D01-MANUAL-20260308-SC-001`
  - 采购合同：`CODEX-TEST-D01-MANUAL-20260308-PC-001`
  - 销售订单：`CODEX-TEST-D01-MANUAL-20260308-SO-001`
  - 采购订单备注：`CODEX-TEST-D01-MANUAL-20260308-PO-001`
  - 附件路径：`CODEX-TEST-D01-MANUAL-20260308/supplier/attachment-001.pdf`
- 联调结束后按前缀定向清理；若某条数据未能确认来源，不得扩大范围删除。

## 5. 管理后台全页与跨页手工联调清单

| 编号 | 角色 | 页面 / 路径 | 核心操作 | 重点核对 | 通过标准 |
|---|---|---|---|---|---|
| ADM-MANUAL-01 | `operations` / `finance` / `admin` | `/login` -> `/dashboard` | 打开登录页，分别以三种角色登录，验证未登录访问受保护页面时回跳登录并保留 redirect | 登录提示、角色标签、重定向、浏览器标题 | 登录成功后进入目标页；未登录访问受保护页会跳转登录；中文标题与角色提示一致 |
| ADM-MANUAL-02 | `operations` | `/dashboard` | 回看履约、资金、库存、异常指标卡，并从首页进入看板或多维报表 | 指标卡中文文案、空态、错误态、跨页入口 | 指标与页面标题正常显示；无英文错误；入口可达 |
| ADM-MANUAL-03 | `operations` | `/board` -> `/funds` / `/funds-reconcile` / `/contract-close` | 从业务看板告警项进入资金、退款核销、合同关闭相关页，再返回看板 | 钻取 query、回填筛选、返回路径、按钮只读边界 | 钻取参数与落地页筛选一致；运营角色不出现财务写按钮 |
| ADM-MANUAL-04 | `finance` / `admin` | `/contracts` | 创建带前缀的采购/销售合同草稿，提交审批、审批通过；回看详情与图谱摘要 | 表单校验、审批流、详情摘要、图谱信息 | 合同可完成“草稿 -> 待审批 -> 生效中”主链；详情与图谱中文提示一致 |
| ADM-MANUAL-05 | `operations` / `finance` / `admin` | `/orders` | 使用同批次销售合同生成订单，执行运营审批、财务审批并绑定采购合同 | 角色按钮边界、审批弹窗、状态流、采购合同绑定 | 运营仅做运营审批，财务仅做财务审批，管理员可兼容回看；状态流与提示正确 |
| ADM-MANUAL-06 | `finance` / `admin` | `/funds` | 回看收款/付款单，补录金额与凭证路径，执行确认 | 付款金额、凭证路径、零付款例外、详情回看 | 非 0 金额必须带凭证；确认后状态与金额展示一致 |
| ADM-MANUAL-07 | `finance` / `admin` | `/funds-reconcile` | 发起退款申请、执行驳回/审核通过/核销链路，回看结果 | 退款状态、核销状态、只读角色阻断 | 财务/管理员可完整闭环；运营仅回看且无写按钮 |
| ADM-MANUAL-08 | `operations` / `finance` / `admin` | `/inventory` | 回看入库/出库列表与详情，确认校验失败提示、系统回执号和手工回执号差异 | 状态筛选、详情摘要、执行异常提示 | 入库/出库切换正常；校验失败与已过账提示清晰 |
| ADM-MANUAL-09 | `finance` / `admin` | `/contract-close` | 回看自动关闭合同，必要时执行手工关闭并核对差异留痕 | 手工关闭条件、差异金额、差异数量、关闭后阻断 | 关闭动作成功后差异展示完整；关闭后相关后续动作被阻断 |
| ADM-MANUAL-10 | `operations` / `finance` / `admin` | `/reports-multi-dim` -> `/reports-export-tasks` -> `/report-recompute-tasks` | 回看多维汇总，验证钻取、导出任务、重算任务、失败重试与只读边界 | 统计口径、导出任务状态、重算结果、角色权限 | 财务/管理员可创建任务并回看结果；运营只读且无越权入口 |

## 6. 小程序手工联调清单

| 编号 | 模式 / 角色 | 页面 | 核心操作 | 重点核对 | 通过标准 |
|---|---|---|---|---|---|
| MINI-MANUAL-01 | `local_api` / `operations`、`finance`、`admin` | 登录页 -> 待办页 -> 快报页 | 切换角色登录，进入待办页，再打开经营快报页 | 身份切换、会话续期、报表权限、消息来源提示 | 本地联调登录成功；角色切换后会话刷新正常；具备权限的角色可见快报 |
| MINI-MANUAL-02 | `local_api` / `customer` | 登录页 -> 待办页 -> 订单页 | 登录客户身份，创建或编辑带前缀订单并提交审批 | 合同下拉、数量校验、提交说明、驳回后再编辑 | 客户仅可访问订单相关能力；空合同/非法数量被阻断 |
| MINI-MANUAL-03 | `local_api` / `warehouse` | 登录页 -> 待办页 -> 执行页 | 登录仓库身份，回看入库/出库回执并提交执行结果 | 仓库边界、回执号、执行结果摘要 | 仓库仅可进入执行链路；中文状态与摘要正确 |
| MINI-MANUAL-04 | `local_api` / `supplier` | 登录页 -> 待办页 -> 消息页 -> 采购进度页 | 登录供应商身份，回看真实采购订单、登记前缀附件路径、在待确认状态下提交单笔发货确认 | 待办摘要、消息深链、附件摘要、付款校验结果 | 供应商无经营金额权限；采购进度、附件与发货确认能力可用 |
| MINI-MANUAL-05 | `wechat_auth` / 未绑定账号 | 登录页 | 使用微信登录，记录 `debug_openid` | 未绑定提示、`debug_openid`、无异常跳转 | 显示“未绑定业务角色”中文提示；不写入会话、不误跳业务页 |
| MINI-MANUAL-06 | `wechat_auth` / 已绑定账号 | 登录页 -> 待办页 / 快报页 / 采购进度页 | 先绑定账号，再执行微信登录并回看对应业务页 | 绑定结果、真实登录、权限边界、会话续期 | 已绑定账号可正常进入对应业务页；权限边界与本地联调一致 |

### 6.1 微信登录绑定参考命令
```bash
cd backend
conda activate jgport
python scripts/bootstrap_mini_program_account.py \
  --openid <debug_openid> \
  --role-code finance \
  --company-id CODEX-TEST-D01-MANUAL-20260308-OPERATOR \
  --company-type operator_company \
  --display-name CODEX-TEST-D01-MANUAL-20260308-FINANCE
```

## 7. 端到端同批次经营链路核对清单

| 编号 | 主链阶段 | 输入 | 核对点 | 预期结果 |
|---|---|---|---|---|
| FLOW-MANUAL-01 | 合同创建与审批 | 同批次采购/销售合同 | 合同状态、图谱摘要、审批留痕 | 合同可生效并带出后续订单入口 |
| FLOW-MANUAL-02 | 客户下单与双审批 | 同批次销售订单 | 订单状态、采购合同绑定、审批意见 | 订单推进到已衍生采购订单 |
| FLOW-MANUAL-03 | 资金补录与确认 | 同批次收款/付款单 | 金额、凭证、退款、核销 | 资金状态与订单/合同金额链路一致 |
| FLOW-MANUAL-04 | 仓储执行 | 同批次入库/出库单 | 回执号、过账状态、校验失败提示 | 履约数量累计正确，异常可追溯 |
| FLOW-MANUAL-05 | 合同关闭 | 数量履约完成且资金闭环 | 自动关闭或手工关闭差异 | 关闭后阻断一致，差异留痕完整 |
| FLOW-MANUAL-06 | 报表与小程序回看 | 上述同批次数据 | 仪表盘、看板、轻量报表、多维报表、待办消息 | 指标与链路数据一致，异常入口可反查 |

## 8. 审计与追溯回看清单
- 业务日志：按同批次前缀回看合同审批、资金确认、合同关闭、异常阻断等日志。
- 报表快照：确认同批次链路影响已进入仪表盘 / 轻量报表快照口径。
- 导出任务：确认导出任务中心中可回看任务状态、结果下载与失败重试。
- 汇总重算：确认重算任务历史、状态、触发人与失败信息可回看。
- 扫描留痕：确认扫描告警与合同关闭相关异常可按测试前缀反查。

## 9. 执行记录模板
- 执行日期：`____-__-__`
- 执行人：`____________`
- 使用数据前缀：`______________________________`
- 通过项：`______________________________`
- 阻断问题：`无 / 详见问题单`
- 高优先级问题：`无 / 详见问题单`
- 留痕路径：`截图 / 录屏 / 数据查询结果 / 文档回写位置`

## 10. 当前结论
- 本清单用于承接阶段 D 剩余手工联调工作，当前仅补齐执行入口与核对口径，不代表手工联调已完成。
- 真机 HTTPS、小程序真实登录账号、微信 `AppID` 与合法域名仍属于执行前置条件；若缺任一项，应先补环境，不得把“环境不可执行”误记为“链路通过”。
