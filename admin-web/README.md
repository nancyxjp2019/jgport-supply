# V6 管理后台工程

## 当前状态
- 管理后台当前代码主要承接旧版 `M8` 首批“可联调”范围，不再代表当前生产化主线。
- 技术基线：`Vue 3 + Vite + TypeScript + Element Plus`。
- 工程组织：布局、路由、权限与页面分层对齐 `Vben Admin` 思路，不直接整仓引入上游 Monorepo 模板。
- 当前 `demo/proxy` 仅用于开发预览与联调，不属于正式交付或生产运行能力；正式治理口径以 `docs/V6生产可用版本治理总纲.md` 为准。
- 当前仓库主线已切换为 `G1 -> G2 -> G3 -> G4 -> G5` 的生产可用版本治理路径；本 README 中的 `M7/M8` 描述仅用于说明旧版已落地的前端基础。
- V5 后台代码已归档至 `archive/v5/admin-web/`。

## 已落地范围
1. 管理后台前端工程初始化。
2. `M7` 首批页面：仪表盘、业务看板。
3. 演示数据模式与代理联调模式切换。
4. 管理后台登录页、路由守卫与代理身份读取。
5. `M8-16` 运营订单处理台首批：销售订单列表、详情回看、运营审批、财务审批。
6. `M8-17` 财务资金处理台首批：收款单/付款单列表、详情回看、手工补录、单笔确认、凭证路径回看。
7. `M8-18` 库存执行跟踪台首批：入库单/出库单列表、详情回看、校验失败回看、执行异常提示。
8. `M8-19` 合同关闭差异台首批：合同关闭列表、详情回看、手工关闭入口、关闭差异留痕回看。
9. `M8-20` 退款核销与资金驳回台首批：退款待审核、退款驳回、退款审核通过与单据核销。
10. `M8-21` 多维报表与导出首批：合同方向/单据状态/退款状态多维汇总、筛选与CSV导出。
11. `M8-22` 按钮级权限首批：关键页面动作按钮按运营/财务/管理员角色收口。
12. `M8-23` 后端细粒度鉴权配套首批：报表导出后端权限收口、资金读写后端权限与页面边界对齐。
13. `M8-24` 导出编排任务中心首批：多维报表导出任务创建、任务历史回看、结果下载与失败重试。

## 本地启动
```bash
cd admin-web
pnpm install
pnpm dev
```

## 环境变量
- `VITE_REPORTS_MODE`：`demo` 或 `proxy`
- `BACKEND_API_BASE_URL`：后端接口地址，默认 `http://127.0.0.1:8000/api/v1`
- `BACKEND_AUTH_SECRET`：仅供 Vite 开发代理注入到后端请求头，禁止放入浏览器端公开变量

## 下一步建议
1. 先进入 `G1`，补齐组织、用户、主数据、审批模板与参数配置页面。
2. 再进入 `G2`，把管理后台正式认证从开发代理模式收口到 `SUPER_ADMIN + MFA`。
3. `M8` 体系下的其他增强项仅作为后续评估池，不替代当前生产化主线。
